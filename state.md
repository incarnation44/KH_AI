# State (실시간 진행 상태판)

> **주의**: 이 파일은 매 작업(또는 세션)이 완료될 때마다 최신 상태로 덮어쓰기(Overwrite)됩니다.

---

## 1. 현재 체크포인트 (2026-09-03 기준)
* **현재 학습 과목**: `02_파이썬_Numpy_Pandas` ➡ `파이썬기초수업자료` ➡ `01.html` (1일차 개발환경과 기본 문법)
* **현재 작업 디렉터리**: `D:\KH_AI\02_파이썬_Numpy_Pandas\파이썬기초수업자료\파이썬기초_예제소스코드`
* **적용 가상환경**: 윈도우 Anaconda `pytest (Python 3.11)`

---

## 2. 방금 완료한 작업
1. 윈도우 아나콘다 `pytest (Python 3.11)` 가상환경 생성 및 VS Code 연동 완료.
2. `d1_test1.py` 작성 및 파이썬 3.11로 터미널 실행 성공.
   * 변수 선언, `print()` 기본 출력 확인.
   * `type()` 함수로 정수(`int`), 실수(`float`), 문자열(`str`), 불리언(`bool`) 4개 기본 자료형 출력 검증 완료.
3. 깃허브 계정 `incarnation44` 로그인 및 연동 확인 완료.

---

## 3. 바로 다음에 진행할 작업 (Next Step)
* 교재 `01.html`의 다음 단원:
  * **연산자(Operator)** 실습 (`d1_op_01.py` ~ `d1_op_05.py`) : 사칙연산, 비교, 논리 연산
  * **문자열 다루기(String)** 실습 (`d1_str_01.py` ~ `d1_str_05.py`) : 슬라이싱, 포맷팅
