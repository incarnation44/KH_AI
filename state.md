# State (실시간 진행 상태판)

> **주의**: 이 파일은 매 작업(또는 세션)이 완료될 때마다 최신 상태로 덮어쓰기(Overwrite)됩니다.

---

## 1. 현재 체크포인트 (2026-09-03 23:05 기준)
* **💻 현재 작업 기기**: **서브 노트북 (Ildo-Laptop / Dell Latitude 7440)** (메인 본체 및 학원 PC와 명확히 분리!)
* **현재 학습 과목**: `02_파이썬_Numpy_Pandas` ➡ `파이썬기초수업자료` ➡ `01.html` (1일차 개발환경과 기본 문법)
* **현재 작업 디렉터리**: `C:\KH_AI\02_파이썬_Numpy_Pandas\파이썬기초수업자료\파이썬기초_예제소스코드` (노트북 NVMe 단일 드라이브 경로)
* **적용 가상환경**: 윈도우 Miniconda `pytest (Python 3.11.16)` 완벽 연동 (VS Code 터미널 자동 진입 검증 완료)

---

## 2. 방금 완료한 작업
1. **💻 서브 노트북(Dell Latitude 7440) 학원 100% 동일 환경 구축 완료**:
   * 윈도우용 경량 Conda(Miniconda) 설치 및 `pytest (Python 3.11.16)` 가상환경 생성.
   * 필수 패키지 5종 전수 설치: NumPy(2.4.6), Pandas(3.0.5), OpenCV(5.0.0), PyTorch(2.14.0 CPU), PyTest(9.1.1).
   * `test.py` 스크립트 실행으로 4개 핵심 모듈 정상 구동 확인 완료.
   * VS Code 상단 Git 부모 폴더 탐색 팝업 영구 차단 및 터미널 진입 시 `(pytest)` 자동 활성화 연동.
   * WSL 우분투 환경의 `~/kdt-linux`를 `C:\KH_AI\kdt-linux`와 1:1 심볼릭 링크로 연결하여 리눅스 터미널 복습 환경 완비.
2. **학원 PC(user1) 최신 커밋 7건 동기화**:
   * `foundation.md`, `decisions.md`, `state.md`, `learnings.md`, `RULES.md` 및 교재/예제 소스 최신화.
2. **`d1_test1.py` 1일차 핵심 문법 전반부 완벽 정복**:
   * 변수 선언 및 `type()` 함수를 통한 기본 자료형 4종(`int`, `float`, `str`, `bool`) 식별.
   * 단순 대입(`=`)과 복합 누적 대입(`+=`)의 메모리 동작 메커니즘 숙지.
   * 다중 대입(`x, y = 120, 85`), 파이썬 전용 스왑(`x, y = y, x`), 연쇄 대입(`w = h = 0`).
   * 산술 연산자 7종 (`+`, `-`, `*`, `/`, `//`, `%`, `**`) 및 나눗셈 3형제(`/` 실수, `//` 정수 몫, `%` 나머지) 원리 이해.
   * 비교 연산자(`==`, `!=`, `>`, `<`, `>=`, `<=`)와 논리 연산자(`and`, `or`, `not`, `in`)를 통한 불리언(`True`/`False`) 판별.
   * 소수점 반올림 다듬기 `round(ratio, 4)` 및 줄바꿈 `\n` 적용.
3. **인공지능 핵심 개념 터득**:
   * 머신러닝 데이터 3분할(Train 70%, Val 15%, Test 15%)의 이유: 과적합(Overfitting) 방지 및 일반화(Generalization) 실력 검증.
4. **시스템 및 환경 정비**:
   * 바탕화면의 `AI_Chat_Logs.md`를 내부 스크래치 보관소로 안전하게 이동하여 바탕화면 완전 클린화.
   * AI 튜터 헌법 및 4대 컨텍스트 파일을 `KH_AI` 및 `obsidian-vault` 원격 저장소에 완벽 분리 반영.

---

## 3. 바로 다음에 진행할 작업 (Next Step)
* 교재 `01.html`의 후반부 진도:
  * **문자열 다루기(String)** 실습 (`d1_str_01.py` ~ `d1_str_05.py`) : 인덱싱, 슬라이싱, 포맷팅
  * **입력과 출력(I/O)** 실습 (`d1_io_01.py` ~ `d1_io_05.py`) : `input()`, 사용자 입력 처리
* 2일차 제어문(조건문 `if`, 반복문 `for` / `while`, 자료구조) 대비
