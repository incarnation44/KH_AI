# State (실시간 진행 상태판)

> **주의**: 이 파일은 매 작업(또는 세션)이 완료될 때마다 최신 상태로 덮어쓰기(Overwrite)됩니다.

---

## 1. 현재 체크포인트 (2026-09-03 기준)
* **현재 학습 과목**: `02_파이썬_Numpy_Pandas` ➡ `파이썬기초수업자료` ➡ `01.html` (1일차 개발환경과 기본 문법)
* **현재 작업 디렉터리**: `D:\KH_AI\02_파이썬_Numpy_Pandas\파이썬기초수업자료\파이썬기초_예제소스코드`
* **적용 가상환경**: 윈도우 Anaconda `pytest (Python 3.11)` 완벽 연동

---

## 2. 방금 완료한 작업
1. **윈도우 아나콘다 `pytest (Python 3.11)` 환경 확정**:
   * VS Code 인터프리터를 `pytest (3.11.16)`으로 선택하고 터미널 `(pytest)` 활성화 완료.
2. **`d1_test1.py` 1일차 핵심 문법 전반부 완벽 정복**:
   * 변수 선언 및 `type()` 함수를 통한 기본 자료형 4종(`int`, `float`, `str`, `bool`) 식별.
   * 단순 대입(`=`)과 복합 누적 대입(`+=`)의 메모리 동작 메커니즘 숙지.
   * 다중 대입(`x, y = 120, 85`), 파이썬 전용 스왑(`x, y = y, x`), 연쇄 대입(`w = h = 0`).
   * 산술 연산자 7종 (`+`, `-`, `*`, `/`, `//`, `%`, `**`) 및 나눗셈 3형제(`/` 실수, `//` 정수 몫, `%` 나머지) 원리 이해.
   * 비교 연산자(`==`, `!=`, `>`, `<`, `>=`, `<=`)와 논리 연산자(`and`, `or`, `not`, `in`)를 통한 불리언(`True`/`False`) 판별.
   * 소수점 반올림 다듬기 `round(ratio, 4)` 및 줄바꿈 `\n` 적용.
3. **인공지능 핵심 개념 터득**:
   * 머신러닝 데이터 3분할(Train 70%, Val 15%, Test 15%)의 이유: 과적합(Overfitting) 방지 및 일반화(Generalization) 실력 검증.
4. **시스템 및 환경 정비**:
   * 바탕화면의 `AI_Chat_Logs.md`를 내부 스크래치 보관소로 안전하게 이동하여 바탕화면 완전 클린화.
   * AI 튜터 헌법 및 4대 컨텍스트 파일을 `KH_AI` 및 `obsidian-vault` 원격 저장소에 완벽 분리 반영.

---

## 3. 바로 다음에 진행할 작업 (Next Step)
* 교재 `01.html`의 후반부 진도:
  * **문자열 다루기(String)** 실습 (`d1_str_01.py` ~ `d1_str_05.py`) : 인덱싱, 슬라이싱, 포맷팅
  * **입력과 출력(I/O)** 실습 (`d1_io_01.py` ~ `d1_io_05.py`) : `input()`, 사용자 입력 처리
* 2일차 제어문(조건문 `if`, 반복문 `for` / `while`, 자료구조) 대비
