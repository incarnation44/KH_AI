# [목적] 조건이 유지되는 동안 반복하며 값을 누적한다
# [설명] 반복 안에서 조건 변수가 반드시 변해야 합니다.

total = 0
i = 1

while i <= 5:
    total += i          # 누적 합
    print(f"i={i}, 누적합={total}")
    i += 1              # 이 줄이 없으면 무한 루프!

print("최종 합계:", total)
